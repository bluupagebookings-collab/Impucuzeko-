{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key. Declared in .env.example but not currently referenced by the frontend source, so it is not required to boot. Provide only if AI features are added later.",
      "requiredAtBoot": false
    }
  ]
}
